# [ADD] Migrated from Scaf-GRPO/sh; keep original experiment settings unless required by verl 0.7.
# #!/usr/bin/env bash
# set -x
# set -euo pipefail

# SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# export EXP_NAME="${EXP_NAME:-outputs/qwen25_math15b_expert_wo_sft}"
# export sft_loss_coef="${sft_loss_coef:-0.0}"

# exec "${SCRIPT_DIR}/qwen25_math15b_expert_sft.sh" "$@"
